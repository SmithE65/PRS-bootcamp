
# Copy this file to `.env` and fill with secure values. Do NOT commit your `.env` file.
# Example values for local development (change them before production):

# Keycloak admin credentials (used to initialize the dev Keycloak instance)
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=admin

# Keycloak image override (optional)
KEYCLOAK_IMAGE=quay.io/keycloak/keycloak:25.0.2

# Keycloak DB settings
KEYCLOAK_DB=postgres
KEYCLOAK_DB_NAME=keycloak
KEYCLOAK_DB_USER=keycloak
KEYCLOAK_DB_PASSWORD=password
KEYCLOAK_DB_PORT=5432

# PRS application DB settings
PRS_DB_NAME=app_db
PRS_DB_USER=app_user
PRS_DB_PASSWORD=app_password

# SQL Server (replaces prs-postgres) - copy values into your local `.env` if using SQL Server
# Strong SA password is required by SQL Server (at least 8 characters with uppercase, lowercase, and numbers/symbols)
PRS_SQL_IMAGE=mcr.microsoft.com/mssql/server:2019-latest
PRS_SQL_SA_PASSWORD=YourStrong!Passw0rd
PRS_SQL_PID=Developer
PRS_SQL_PORT=1433
PRS_SQL_DB=AppDb

# Optional: Angular dev server origin (used for CORS reference)
# ANGULAR_ORIGIN=http://localhost:4200

# Notes:
# - After copying this file to `.env` and customizing values, run `docker compose up -d`.
# - Keep `.env` out of version control. Use CI/CD secrets or an external secret manager for production.
